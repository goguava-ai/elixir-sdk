# Copy this file to `.env` (gitignored) and fill in real values.
# Loaded automatically by the test suite and passed into the Docker toolchain
# by `./emix`. Used by live tests: `./emix test --include live`.
#
# Do NOT quote values (Docker's --env-file treats quotes literally).

GUAVA_API_KEY=gva-your-key-here
GUAVA_AGENT_NUMBER=+14155550123
